# كيف تحرّر ديوانك؟ (دليل سريع)

كل شيء في موقع «أَثَر» يُدار من ملفٍ واحد:
**`athar-site/data/poems.json`** — لا تحتاج لمس أي ملف آخر.

---

## 1) تعديل بيتٍ موجود

1. افتح `data/poems.json` بأي محرر نصوص (حتى المفكرة).
2. ابحث عن عنوان القصيدة داخل `"title"`.
3. داخل `"verses"` ستجد الأبيات بهذا الشكل:

```json
"verses": [
  { "sadr": "الشطر الأول (الصدر)", "ajoz": "الشطر الثاني (العجز)" },
  { "sadr": "بيتٌ ثانٍ…",            "ajoz": "…وعجزُه" }
]
```

4. عدّل النص بين علامتي التنصيص فقط، واحفظ الملف.
5. ارفع الملف المعدّل إلى منصة النشر (أو اسحبه من جديد إلى Netlify Drop) فيتحدّث الموقع.

> قاعدة ذهبية: لا تحذف الفواصل `,` ولا الأقواس `{ } [ ]` ولا علامات التنصيص.
> وإن أردت كتابة علامة تنصيص `"` داخل نصّ بيت، فاكتبها هكذا: `\"`

---

## 2) إضافة قصيدة جديدة

انسخ هذا القالب والصقه **داخل قائمة `"poems"`** بعد آخر قصيدة
(ضع فاصلة `,` بعد قوس الإغلاق `}` للقصيدة السابقة):

```json
{
  "id": "esim-latini-fared",
  "title": "عنوان القصيدة",
  "category": "وطن",
  "year": "2026",
  "mood": "جملةٌ تصف جوّ القصيدة",
  "excerpt": "سطرٌ تعريفيّ قصير يظهر في البطاقة.",
  "accent": "olive",
  "tags": ["كلمة", "أخرى"],
  "featured": false,
  "verses": [
    { "sadr": "الصدر الأول", "ajoz": "العجز الأول" },
    { "sadr": "الصدر الثاني", "ajoz": "العجز الثاني" }
  ]
}
```

### شرح الحقول

| الحقل | الوظيفة | إلزامي؟ |
|---|---|---|
| `id` | معرّف فريد (يفضّل لاتيني بلا مسافات) — يُبنى عليه رابط القصيدة | نعم |
| `title` | عنوان القصيدة | نعم |
| `category` | الباب: `مناجاة` / `شوق` / `تأمل` / `وطن` / `فخر` — أو باب جديد يُنشأ تلقائياً | نعم |
| `year` | سنة القصيدة (تظهر شارةً بجانب العنوان) | اختيارى |
| `mood` | سطر الجو تحت العنوان | اختيارى |
| `excerpt` | المقتطف التعريفي في البطاقة | اختيارى |
| `accent` | لون القصيدة: `terracotta` أو `olive` أو `indigo` | اختيارى |
| `tags` | وسوم للبحث | اختيارى |
| `featured` | `true` لجعلها مرشّحة للقسم «قصيدة مختارة» | اختيارى |
| `verses` | الأبيات: كل بيت `{ "sadr": …, "ajoz": … }` | نعم |

### إضافة بابٍ جديد (تصنيف)

أضف عنصراً إلى قائمة `"categories"`:

```json
{ "id": "رثاء", "label": "الرثاء", "description": "فيمن مضوا وبقي أثرهم." }
```

---

## 3) تغيير بيت الواجهة (الاقتباس الكبير)

في أعلى الملف داخل `"site"`:

```json
"heroQuote": {
  "sadr": "غَدَوْتُ سَرَاباً كُلَّمَا رُمْتُ لَمْسَهُ",
  "ajoz": "تَلاشَى وَأَغْرَتْنِي الرُّؤَى وَهْيَ تَكْذِبُ"
}
```

وكذلك يمكنك تغيير: القصيدة المختارة (`"featuredId"`)، عبارات الشريط المتحرك (`"marquee"`)،
السيرة (`"bio"`)، ووسائل التواصل (`"contact"`).

---

## 4) طريقتان للحفظ والنشر

### أ) من داخل الموقع (معاينة فورية بلا ملفات)
التذييل ← **«محرّر البيانات»** ← عدّل الـ JSON ← **«تطبيق وحفظ»** (يُحفظ في متصفحك للمعاينة)
← **«تنزيل poems.json»** ← ضع الملف المنزّل فوق `data/poems.json` في مشروعك وارفعه للنشر.

### ب) تحرير الملف مباشرة
عدّل `data/poems.json` واحفظ، ثم:
- **Netlify Drop:** اسحب مجلد `athar-site` (أو zip منه) مجدداً إلى app.netlify.com/drop
- **GitHub:** ارفع الملف (commit) — يتحدّث الموقع تلقائياً
- **Vercel:** نفس فكرة GitHub

> اختياري: بعد أي تعديل نفّذ `python3 sync_inline.py` لتحديث النسخة المضمّنة
> الاحتياطية داخل `index.html` (تُستخدم فقط عند فتح الملف بدون خادم).

---

## 5) فحص سريع قبل النشر

- افتح الموقع وجرّب البحث عن كلمة من القصيدة الجديدة.
- إن ظهرت رسالة خطأ في «محرّر البيانات» فغالبا فاصلة ناقصة أو زائدة.
- ترتيب العرض = ترتيب القصائد داخل الملف تماماً.

## ٦) لوحة التحكم المدمجة (الأسهل!)

من تذييل الموقع اضغط **«لوحة التحكم»** (أو أضف `#admin` لرابط الموقع).
رمز الدخول الافتراضي: **athar2026** — غيّره من تبويب «النسخ والنشر».

- **تبويب القصائد:** إضافة قصيدة جديدة، تعديل أي قصيدة وأبياتها سطراً سطراً
  (صدر/عجز مع تقديم/تأخير/حذف للأبيات)، ترتيب القصائد (↑ ↓)، تكرار، حذف.
- **تبويب الواجهة والإعدادات:** بيت الواجهة، القصيدة المختارة، الشريط المتحرك،
  السيرة والنقاط، اسم الشاعر، صورة الواجهة، ووسائل التواصل.
- **تبويب النسخ والنشر:** تنزيل `poems.json` محدَّثاً، استيراد ملف، استعادة المنشور،
  وتغيير رمز الدخول.

**حفظ التغييرات** يخزنها في متصفحك ويحدّث الموقع فوراً للمعاينة؛
ولكي يراها كل الزوار: نزّل `poems.json` من التبويب الثالث وارفعه مع المشروع لمنصتك.
